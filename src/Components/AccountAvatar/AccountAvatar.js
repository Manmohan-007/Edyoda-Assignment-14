import React from 'react'import classes from './AccountAvatar.module.css'import Profile from '../../Assets/Images/Profile.png'import ButtonGenerator from "../ButtonGenerator/ButtonGenerator";import { Bin } from "../../Assets/Icons";import { ImageType } from "../../Utils/Constant";const AccountAvatar = () => {    let displayImage = Profile    let fileSize = 0    const changeFile = () => {        var ele = document.getElementById('fileSelector')        ele.click()    }    let newProfile = (e) => {        let size = Math.round(e.currentTarget.files[0].size/1024)        if(size <= 1024) {            fileSize = size        } else {            alert('Please select a file of size less than 1MB')        }    }    return(        <div className={classes.MainContainer}>            <h4 className={classes.MainTitle}>Change Avatar</h4>            <div className={classes.AvatarImageContainer}>                <img className={classes.AvatarImage} src={displayImage} alt='user_avatar'/>                <div className={classes.ChangeImageOverlay}>                    <div className={classes.Overlay}></div>                    <div className={classes.BinContainer}>                        {Bin}                    </div>                </div>                <input onChange={(e) => newProfile(e)} id={'fileSelector'} className={classes.FileSelector} name={'Photo'} type={'file'} accept={ImageType}/>            </div>            <div onClick={() => changeFile()} className={classes.ButtonContainer}>                <ButtonGenerator BtnTitle={'Upload New Photo'}/>            </div>        </div>    )}export default AccountAvatar