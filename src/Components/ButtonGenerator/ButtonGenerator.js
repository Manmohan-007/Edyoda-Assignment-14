import React from "react";import classes from './ButtonGenerator.module.css'const ButtonGenerator = (props) => {    let type = props.BtnType    if(props.BtnType === undefined) {        type = 'button'    }    const handleUpdate = () => {        if(type != 'button') {            props.handle()        }        if(props.BtnTitle == 'Update Your Profile') {            props.updateData()        }        if(props.BtnTitle == 'Delete Your Account') {            props.deleteData()        }        if(props.BtnTitle == 'Add New Product') {            props.history.push('/add_product')        }        if(props.BtnTitle == 'Delete Selected Products') {            props.multiDelete()        }        if(props.BtnTitle == 'Add Category') {            props.handle()        }    }    return(        <button onClick={(e) => handleUpdate()} type={type} className={classes.MainContainer} aria-required>{props.BtnTitle}</button>    )}export default ButtonGenerator