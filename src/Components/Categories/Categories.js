import React from "react";import classes from './Categories.module.css'import { connect } from 'react-redux'import { Bin } from "../../Assets/Icons";const Categories = (props) => {    const deleteCategory = () => {        let data = props.data        data.productsPage.categories.splice(props.itemId,1)        props.deleteProduct(data)    }    return(        <div className={classes.MainContainer}>            <p className={classes.CategoryName}>{props.CategoryName}</p>            <div onClick={() => deleteCategory()} className={classes.BinContainer}>{Bin}</div>        </div>    )}let fetchDataFromGlobalStore = (globalStore) => {    return globalStore.allData}let updateAllData = (dispatch) => {    return {        deleteProduct: (updatedData) => dispatch({type: 'DELETE_PRODUCT', data: updatedData})    }}export default connect(fetchDataFromGlobalStore, updateAllData)(Categories)