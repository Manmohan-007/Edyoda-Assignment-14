import React from 'react'import { connect } from 'react-redux'import classes from './Notifications.module.css'let Notiifications = (props) => {    let notificationArray = props.notifications    return(        <div className={classes.MainContainer}>            <div className={classes.MainTitle}>Notification List</div>            {                notificationArray.map((item, index) => {                    return (                        <div key={index} id={index} className={classes.EachNotification}>                            <div className={classes.SenderImageContainer}>                                <img className={classes.SendersImage} src={item.pic} alt='user_icon'/>                            </div>                            <div className={classes.DescriptionContainer}>                                <p className={classes.Message}>{item.message}</p>                                <p className={classes.MessageTime}>{item.time}</p>                            </div>                        </div>                    )                })            }        </div>    )}let fetchDataFromGlobalStore = (globalStore) => {    return globalStore.allData.data.dasbhoardPage}export default connect(fetchDataFromGlobalStore)(Notiifications)