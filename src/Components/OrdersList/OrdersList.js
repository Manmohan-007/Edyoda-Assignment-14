import React from "react";import classes from './OrdersList.module.css'import { connect } from 'react-redux'import OrderDetails from "../OrderDetails/OrderDetails";let OrdersList = (props) => {    let orders = props.orders    return(        <div className={classes.MainContainer}>            <h2 className={classes.MainTitle}>Orders List</h2>            <table className={classes.TableContainer}>                <tr className={classes.HeaderRow}>                    <th className={classes.TableHeadings}>Order No.</th>                    <th className={classes.TableHeadings}>Status</th>                    <th className={classes.TableHeadings}>Operations</th>                    <th className={classes.TableHeadings}>Location</th>                    <th className={classes.TableHeadings}>Distance</th>                    <th className={classes.TableHeadings}>Start Date</th>                    <th className={classes.TableHeadings}>Est Delivery Time</th>                </tr>                {                    orders.map((item, index) =>                        {                            return <OrderDetails ID={index} {...props} {...item}/>                        }                    )                }            </table>        </div>    )}let fetchDataFromGlobalStore = (globalStore) => {    return globalStore.allData.data.dasbhoardPage}export default connect(fetchDataFromGlobalStore)(OrdersList)