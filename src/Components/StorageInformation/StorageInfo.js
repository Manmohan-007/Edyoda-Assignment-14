import React , { useEffect } from "react";import classes from './StorageInfo.module.css'import { connect } from 'react-redux'import { Pie } from "react-chartjs-2";const StorageInfo = (props) => {    let keys = Object.keys(props.storage)    let values = Object.values((props.storage))    let labelObj = []    keys.map((item,index) => {        labelObj.push(            `${item.charAt(0).toUpperCase()}${item.substr(1)} Storage (${props.storage[item]}GB)`        )    })    return(        <div className={classes.MainContainer}>            <h4 className={classes.MainTitle}>Storage Information</h4>            <Pie className={classes.Chart}                data={{                    labels: labelObj,                    datasets: [{                        data: values,                        backgroundColor: [                            '#F7604D',                            '#4ED6B8',                            '#A8D583'                        ],                        borderWidth: 0.4                    }],                }}                options= {{                legend: {                    labels: {                        fontColor: '#fff',                        fontSize: 12                    }                }            }}                 height='200%'            />        </div>    )}let fetchDataFromGlobalStore = (globalStore) => {    return globalStore.allData.data.dasbhoardPage}export default connect(fetchDataFromGlobalStore)(StorageInfo)