import React from 'react'import classes from './LoginPage.module.css'import { connect } from "react-redux";import ButtonGenerator from "../../Components/ButtonGenerator/ButtonGenerator";const LoginPage = (props) =>  {    let username = ''    let password = ''    let inputData = (e) => {        switch (e.target.name) {            case 'username': return username = e.target.value            case 'password': return password = e.target.value            default: return null        }    }    const handleLog = (e) => {        if(username !== '' && password !== '') {            props.history.push('dashboard')            props.changeActivePage('dashboard')            props.logIn()        }    }    return(        <div className={classes.MainContainer}>            <form className={classes.FormContainer}>                <caption className={classes.FormTitle}>Welcome to Dashboard, Login</caption>                <label className={classes.FormLabels}>Username</label>                <input onChange={(e) => inputData(e)} className={classes.InputFields} name='username' type='text' required/>                <label className={classes.FormLabels}>Password</label>                <input onChange={(e) => inputData(e)} className={classes.InputFields} name='password' type='password' required/>                <div className={classes.ButtonContainer}>                    <ButtonGenerator handle={(e) => handleLog(e)} BtnTitle={'Login'} BtnType={'submit'}/>                    <ButtonGenerator BtnTitle={'Forgot Password'} />                </div>            </form>        </div>    )}let updateUserLog = (dispatch) => {    return {        logIn : () => dispatch({type: 'LOGGING_IN'})    }}export default connect( '',updateUserLog)(LoginPage)