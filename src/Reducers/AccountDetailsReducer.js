let initialState = {}export const AccountDetails = (state = initialState, action) => {    switch (action.type) {        case 'UPDATE_PROFILE': return {            ...state,            data: action.data        }        case 'DELETE_PROFILE': return {            ...state,            data: action.updatedData        }        default: return {            ...state        }    }}